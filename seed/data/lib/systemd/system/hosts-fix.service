[Unit]
Description=Hosts fix
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c "cat /etc/hosts | sed 's/localhost.localdomain localhost/localhost localhost.localdomain/g' | tee /etc/hosts > /dev/null"

[Install]
WantedBy=multi-user.target
