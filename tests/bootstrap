#!/bin/bash

[ "$TEST" = 'php' ] && wget https://get.webino.org/seed/php -O - | sh
[ "$TEST" = 'devkit' ] && wget https://get.webino.org/devkit -O - | sh
# TODO apache
#[ "$TEST" = 'apache' ] && wget https://get.webino.org/seed/apache -O - | sh
# TODO seed
#[ "$TEST" = 'seed' ] && wget https://get.webino.org/seed -O - | sh
# TODO easy
#[ "$TEST" = 'easy' ] && wget https://get.webino.org -O - | sh

exit 1
