#!/bin/bash

# This is the Webino™ setup script!
# Copyright (c) 2015-2018 Webino, s.r.o. (http://webino.sk)
# Version: @@version@@
#
# Are you looking at this in your web browser, and would like to get Webino™?
# Just open up your terminal and type:
#
#    wget @@host@@/php -qO- | sh
#
# Webino™ currently supports:
#   - Ubuntu 16.04 | 15.10 | 14.04
#
# What this script does?
#   - It builds PHP from source

main() {
    cd /tmp/php
    local etc='/etc/'${php_prefix:-'php'}
    local data='@@host@@/data/etc'

    echo 'Configuring PHP...'

    sudo mkdir -p $etc/conf.d

    [[ -n $update ]] &&
        rm $etc/php.ini \
        && rm $etc/conf.d/custom.ini \
        && rm $etc/php-cli.ini

    # php config
    [[ -f $etc/php.ini ]] || \
        sudo cp ./php.ini-production $etc/php.ini

    sudo pear config-set php_ini $etc/php.ini
    sudo pecl config-set php_ini $etc/php.ini

    # cli config
    [[ -f $etc/php-cli.ini ]] || \
        sudo wget -qO $etc/php-cli.ini $data/php/php-cli.ini

    # custom config
    [[ -f $etc/conf.d/custom.ini ]] || \
        sudo wget -qO $etc/conf.d/custom.ini $data/php/conf.d/custom.ini

    # local & dev only
    if [[ -n $env_local || -n $env_dev ]]; then

        # dev config
        [[ -f $etc/conf.d/dev.ini ]] || \
            sudo wget -qO $etc/conf.d/dev.ini $data/php/conf.d/dev.ini
    fi

    # apache config
    [[ ! -d /etc/apache2/conf.d || -f /etc/apache2/conf.d/php.conf ]] || \
        sudo wget -qO /etc/apache2/conf.d/php.conf $data/apache2/conf.d/php.conf

    return
}

main
