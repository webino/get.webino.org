/srv/*/log*/*.log {
    weekly
    missingok
    rotate 7
    compress
    delaycompress
    notifempty
    create 644 root adm
    sharedscripts
    prerotate
        if [ -d /etc/logrotate.d/httpd-prerotate ]; then \
            run-parts /etc/logrotate.d/httpd-prerotate; \
        fi; \
    endscript
}
