language: bash
env:
  - TEST=apache
  - TEST=apache-pub
  - TEST=mysql
  - TEST=php
  - TEST=php7
  - TEST=devkit
  - TEST=dev-module
matrix:
  allow_failures:
    - env: TEST=apache-pub
before_install:
  - sudo chmod o+w /home/travis -R
script:
  - tests/bootstrap
